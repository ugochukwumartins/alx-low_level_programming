#include "main.h"
#include <stdio.h>

/**
 * _abs -> Computes the absolute value of an integer
 * @n: parameter
 * Return: absolute interger
 */
int _abs(int n)
{
	if (n < 0)
		n = -n;
			return (n);
}
