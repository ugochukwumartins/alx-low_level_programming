#include "main.h"
#include <stdio.h>
/**
 * add -> adds two intergers
 * @i: parameter 1
 * @j: parameter 2
 * Return: Returns the result
 */
int add(int i, int j)
{
	int res;

	res = (i + j);
	return (res);
}
